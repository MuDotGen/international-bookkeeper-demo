# international-bookkeeper

npm run build
firebase deploy

Use the .github\workflows\firebase-hosting-merge.yml file to set the build and deploy commands to first change directories to the root at international-bookkeeper and the entry-point to the same root folder that has the firebase.json file.
